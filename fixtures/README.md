# Fixtures

Sanitized, deterministic recorded cases and external-service contract fixtures live here.

Fixtures must contain no credentials or private account identifiers and must preserve enough provenance to reproduce each decision safely without network access.
