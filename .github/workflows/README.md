# Workflows

GitHub Actions workflows for backend checks, frontend checks, secret scanning, and aggregate verification live here.

Workflow files are added with their corresponding verified build tasks; generated dependencies and credentials do not belong in this directory.
