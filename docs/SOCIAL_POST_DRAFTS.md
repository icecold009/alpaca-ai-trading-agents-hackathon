# Build-in-public post drafts

These drafts are intentionally unpublished. Post only after the release candidate is safe, and replace no wording with live-performance claims unless private evidence supports it. Tag LabLab and Alpaca as required by the event.

## Post 1 — mechanism (X-ready, 259 characters)

RiskCourt treats AI forecasts like a prediction market: independent jurors stake calibrated probabilities, then a deterministic risk court compares jury odds with an option-payoff hurdle. No edge, no fresh data, or no approval means no order. @lablabai @AlpacaHQ

## Post 2 — engineering boundary (X-ready, 265 characters)

The useful part of an AI trading agent is what it refuses to do. RiskCourt binds every paper intent to evidence, policy version, a short approval expiry, and an idempotent client ID. Stale quotes, disagreement, duplicates, and kill switch all fail closed. @lablabai @AlpacaHQ

## Post 3 — honest status (X-ready, 262 characters)

RiskCourt now has a replayable 12-case evaluation set, a judge-facing Decision Card, and verified local browser/security gates. The next proof is deliberately boring: official Alpaca MCP/CLI, a fresh-account paper loop, hosted verification, then submission. @lablabai @AlpacaHQ

## Publishing checklist

- Confirm the public repository and hosted URL are approved and logged-out reachable.
- Link only to public artifacts that contain no secrets, account IDs, order IDs, or private paths.
- Keep “paper”, “simulated”, and “not investment advice” language visible where performance is discussed.
- Save the exact public URL and timestamp privately after posting; add links to the LabLab form only if desired.
